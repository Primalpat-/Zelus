@model List<Zelus.Web.Controllers.Mods.Models.ModVM>

@foreach (var modVM in Model)
{
    <div class="row">
        <div class="col">
            @Html.Partial("_Mod", modVM)
        </div>
    </div>
}
